{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build (Debug)",
            "type": "shell",
            "command": "cd build && cmake .. -DCMAKE_BUILD_TYPE=Debug && make -j && cp compile_commands.json .. && cd ..",
            "problemMatcher": []
        }
    ]
}